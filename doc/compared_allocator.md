这个代码比较了三种调度方法：

- 静态：
    - 纯静态的调度逻辑是，将资源划分为若干分区，任务静态的绑定到这些分区；在每个分区内部，每个周期的时间片被静态的划分成若干个区间，每个分区内的任务，在对应的区间内串行的执行。为了保证任务能够满足延迟约束，分区的大小和每个任务分配的时间片长度，都需要满足对应的分位数延迟约束。反应在代码上，deduce_cfg2 强制所有的任务都保证对应特定概率的分位数（exec_t_comp_ratioA）的延迟，并保证他们的总和满足端到端延迟约束。
  - 优势：得益于加速器double buffer的设计和确定的切换行为，计算密集的任务kernel的启动延迟，可以大部分被隐藏。
  - 劣势：这种per-task 的预留调度方式过于悲观。导致资源利用率低。
- 动态:
  - 不再显式的制定,切换的时间,以及执行的位置。带来的好处是，能够灵活的所有tile上空闲的时间片。但是随之而来的问题是，启动kernel所需的延迟，难以被掩盖。

- 静态+动态：
    - 核心思想是，任务per-task静态的预留方式过于悲观，资源应该以任务组而不是per-task，为粒度预留资源，在组内动态的分配资源可以进一步提高资源利用率，这样即使用更宽松的分位数延迟进行预留，也能满足延迟。
    - Split的功能（coleasing_alloc_cluster）：相对于纯静态，使用更少的分组数量，允许分组内的任务在空间维度上共享资源。
    - repack（push_task_into_bins_new）则是，在固定分区的前提下，在组内为每个任务分配一个时间区间。如果执行落在该区间内，则可以隐藏启动开销，如果不在，则需要动态的分配资源。相对于纯动态，时间区间的分配会更加激进：使用更加宽松的分位数exec_t_comp_ratioB进行预留，因此，ERT和ddl会比静态的方法更早。

当前方案下，coleasing_alloc_cluster，以及push_task_into_bins_new 已经基本上实现了上面的想法。
从头实现，只需要 生成workload，生成静态信息，指定资源，split，repack。

但是，如果需要支持实现消融试验：
  - 只bin-split（【待定】可能需要基于某一exec_t_comp_ratioA得到的资源和时间片的分派结果）
  - 从固定某按照 exec_t_comp_ratioA 资源数量，开始减少资源数量
  - 固定上一步bin-split分区结果 + 使用不同的 exec_t_comp_ratioB 进行 repack


但是，现在看来之前的代码逻辑过于复杂：
    包含了预先加载bin_list，force_core，重新生成workload，等操作。导致反复的取消，重做一些操作。
似乎不能实现上面提到的消融试验。
应该如何进行改进？


