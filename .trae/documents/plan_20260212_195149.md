我已完成对文档和代码的深度分析。
1.  **Spec对齐检查**：`scripts/abla_exp_runner.py` 中的 `AblaExp1Runner`, `AblaExp2Runner`, `AblaExp3Runner` 逻辑与 `doc/spec/e2e_sched_sim_flow.md` 中的定义完全一致。
    *   Case 1: 正确实现了 `cyc` (硬隔离) 与 `cyc-S` (软预留) 的对比，参数扫描范围符合预期。
    *   Case 2: 正确实现了 `glb` (num_bins=1) 与 `pglb` (num_bins>1) 的对比，扫描了 tiles/chains/load。
    *   Case 3: 正确实现了 `reserv` (repack) 与 `pglb` (no repack) 的对比。
2.  **代码复用检查**：脚本通过 `from approach_collector import StatisticsCollector` 成功复用了 motivation 实验的绘图逻辑 (`plot_motiv_case1`, `plot_motiv_case2`) 和基础参数模板，符合“最大化复用”的要求。

为了稳妥地验证并“增加一次成功的几率”，我制定了以下分阶段实施计划：

### 第一阶段：环境确认与Case 1验证
1.  检查 Python 环境（确认 `gurobi` 环境）。
2.  运行 Case 1 实验：`python -m scripts.abla_exp_runner --case 1 --output_dir ./abla_results --num_hp 100`
3.  验证 Case 1 输出结果（JSON 和 PDF 图表）。

### 第二阶段：Case 2验证
1.  运行 Case 2 实验：`python -m scripts.abla_exp_runner --case 2 --output_dir ./abla_results --num_hp 100`
2.  验证 Case 2 输出结果。

### 第三阶段：Case 3验证
1.  运行 Case 3 实验：`python -m scripts.abla_exp_runner --case 3 --output_dir ./abla_results --num_hp 100`
2.  验证 Case 3 输出结果。

如果不顺利，我将按照指示修改脚本设置 `_PHYSICAL_CORES = 1` 进行调试。
